# This is a configuration file for /etc/init.d/sleepd.

# Parameters to pass to sleepd.
PARAMS="--auto --battery=5 --sleep-command='loginctl lock-sessions && sleep 1 && systemctl suspend' --hibernate-command='loginctl lock-sessions && sleep 1 && systemctl hibernate'"
